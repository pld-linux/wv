diff -urN wv-1.0.0.orig/wvHtml.in wv-1.0.0/wvHtml.in
--- wv-1.0.0.orig/wvHtml.in	2002-02-18 09:08:35.000000000 +0100
+++ wv-1.0.0/wvHtml.in	2003-09-20 14:31:26.000000000 +0200
@@ -34,6 +34,9 @@
     --charset=* | --password=*)
       wv_opts="$wv_opts $1"
       ;;
+    -1)
+      wv_opts="$wv_opts $1"
+      ;;
     -v | --version)
       echo @VERSION@
       exit 0
diff -urN wv-1.0.0.orig/wvText.in wv-1.0.0/wvText.in
--- wv-1.0.0.orig/wvText.in	2002-01-16 16:04:12.000000000 +0100
+++ wv-1.0.0/wvText.in	2003-09-20 14:32:07.000000000 +0200
@@ -3,7 +3,7 @@
 prefix=@prefix@
 exec_prefix=@exec_prefix@
 datadir=@datadir@
-tmpdir=/tmp
+tmpdir=${TMPDIR:-/tmp}
 
 # argument checking
 if [ ${#} -ne "2" ]; then
@@ -11,16 +11,20 @@
 	exit 1
 fi
 
-# start out optimistic
-USING_LYNX=1
-which lynx >/dev/null 2>&1
-if [ ${?} -ne "0" ]; then
-	echo "Could not find required program 'lynx'"
-	echo "Not using lynx. Ouput will be pretty bad."
-	USING_LYNX=0
+USE_DUMP=0
+which elinks >/dev/null 2>&1 && USE_DUMP=2 || { which lynx >/dev/null 2>&1 && USE_DUMP=1;}
+
+if [ $USE_DUMP -eq "1" ]; then
+	echo "Could not find required program 'elinks'"
+	echo "Using lynx. Ouput will be pretty ugly."
+fi
+
+if [ $USE_DUMP -eq "0" ]; then
+	echo "Could not find required program 'elinks' or even 'lynx'"
+	echo "Using wvWare -x wvText.xml. Ouput will be pretty bad."
 fi
 
-if [ ${USING_LYNX} -ne "0" ]; then
+if [ $USE_DUMP -gt "0" ]; then
 
     # first, test for wvHtml
     which wvHtml >/dev/null 2>&1
@@ -30,18 +34,26 @@
     fi
 
     # intermediate file
-    TMP_FILE="wv$$.html"
+    TMP_FILE=`mktemp "$tmpdir/wv-XXXXXX"`
+    TMP_FILE=`basename "$TMP_FILE"`
 
-    wvHtml "${1}" --targetdir="${tmpdir}" "${TMP_FILE}" >/dev/null 2>&1
+    wvHtml -1 "${1}" --targetdir="${tmpdir}" "${TMP_FILE}" >/dev/null 2>&1
     if [ ${?} -ne "0" ]; then
 	echo "Could not convert into HTML"
 	exit 1
     fi
 
-    # lynx actually does quite well
-    TERM=vt100 lynx -dump -force_html "${tmpdir}/${TMP_FILE}" > "${2}"
+    if [ $USE_DUMP -eq "2" ]; then
+	# elinks does the best
+	elinks -dump -force-html "${tmpdir}/${TMP_FILE}" > "${2}"
+    else
+	# lynx actually does quite well
+	TERM=vt100 lynx -dump -force_html "${tmpdir}/${TMP_FILE}" > "${2}"
+    fi;
+
     if [ ${?} -ne "0" ]; then
 	    echo "Could not convert into Text"
+	    rm -f "${tmpdir}/${TMP_FILE}"
 	    exit 1
     fi
 
